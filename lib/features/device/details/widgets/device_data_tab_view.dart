import 'package:flutter/material.dart';

class DeviceDataTabView extends StatelessWidget {
  const DeviceDataTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('데이터'),
    );
  }
}
